# Audit report — {{site_name}}

**Audited:** {{date}}
**Scope:** {{scope}} ({{file_count}} files scanned)
**Framework:** {{framework}}

## Score

| Category | Grade | Score | Scored over | Findings |
|---|---|---|---|---|
| SEO  | {{seo_grade}} | {{seo_score}}/100 | {{seo_scored}} of {{seo_total}} checks ({{seo_na}} N/A, {{seo_unverified}} unverified) | {{seo_high}} high · {{seo_medium}} medium · {{seo_low}} low |
| GEO  | {{geo_grade}} | {{geo_score}}/100 | {{geo_scored}} of {{geo_total}} checks ({{geo_na}} N/A, {{geo_unverified}} unverified) | {{geo_high}} high · {{geo_medium}} medium · {{geo_low}} low |
| A11y | not audited   | —                 | —                                                                                     | — |

**Arithmetic:** SEO `{{seo_arithmetic}}` · GEO `{{geo_arithmetic}}`

> {{one_line_summary}}

---

## Top fixes (highest leverage first)

{{#each top_fixes}}
### {{rank}}. {{check_id}} — {{title}}  · *{{severity}}* · {{deduction}}

**Where:** `{{file}}:{{line}}` ({{count}} occurrences across {{file_count}} files)

**What's wrong:** {{description}}

**Fix:**
```{{lang}}
{{fix_snippet}}
```

{{/each}}

---

## All findings

Each line shows the check ID, severity, the deduction actually applied, and the
occurrence count. Deductions follow the per-check-ID ladder in `checks/scoring.md`
— an ID is charged once regardless of how many files fail it.

### SEO ({{seo_findings_count}})

{{#each seo_findings}}
- **{{check_id}}** · *{{severity}}* · {{deduction}} — {{title}} ({{count}} occurrences)
  - `{{file}}:{{line}}` — {{detail}}
{{/each}}

### GEO ({{geo_findings_count}})

{{#each geo_findings}}
- **{{check_id}}** · *{{severity}}* · {{deduction}} — {{title}} ({{count}} occurrences)
  - `{{file}}:{{line}}` — {{detail}}
{{/each}}

---

## Unverified

Checks that apply but cannot be settled from source alone. **These are not
failures** and carry no deduction — most often JSON-LD injected at runtime,
which is present and probably correct but unparseable statically.

{{#each unverified}}
- **{{check_id}}** — {{title}}
  - `{{file}}:{{line}}` — {{reason}}
  - *To settle it:* {{how_to_verify}}
{{/each}}

## Not applicable

Checks excluded from the denominator because they cannot apply to this repo.

{{#each not_applicable}}
- **{{check_id}}** — {{title}} — {{why_na}}
{{/each}}

---

## Methodology

This audit was generated by the `seo-geo-audit` Claude skill. Each check is a
piece of semantic reasoning over the source code, not a regex match — meaning
findings reflect *content quality*, not just *presence*.

Scoring follows `checks/scoring.md`: each category starts at 100, each failing
check ID is charged once on a fixed severity/prevalence ladder, and the result
is floored at 0. Not-applicable checks leave the denominator; unverified checks
are never charged. The arithmetic is printed above so the number can be checked.

The audit is read-only and reads source only — it does not fetch the live site.
To apply fixes, run `/seo-geo-audit fix` and confirm each edit.
